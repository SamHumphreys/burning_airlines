<%= form_for(@aeroplane) do |f| %>
  <% if @aeroplane.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aeroplane.errors.count, "error") %> prohibited this aeroplane from being saved:</h2>

      <ul>
      <% @aeroplane.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rows %><br>
    <%= f.text_field :rows %>
  </div>
  <div class="field">
    <%= f.label :columns %><br>
    <%= f.text_field :columns %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
